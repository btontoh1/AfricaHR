# how-it-works-feature

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build how-it-works-feature` to build the library.

## Running unit tests

Run `nx test how-it-works-feature` to execute the unit tests via [Jest](https://jestjs.io).
