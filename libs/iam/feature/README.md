# iam-feature

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build iam-feature` to build the library.

## Running unit tests

Run `nx test iam-feature` to execute the unit tests via [Jest](https://jestjs.io).
