# payroll-feature

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build payroll-feature` to build the library.

## Running unit tests

Run `nx test payroll-feature` to execute the unit tests via [Jest](https://jestjs.io).
