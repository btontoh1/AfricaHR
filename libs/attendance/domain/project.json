{
  "name": "attendance-domain",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/attendance/domain/src",
  "projectType": "library",
  "tags": ["scope:attendance", "type:domain"],
  "targets": {
    "build": {
      "executor": "@nx/js:tsc",
      "outputs": ["{options.outputPath}"],
      "options": {
        "outputPath": "dist/libs/attendance/domain",
        "main": "libs/attendance/domain/src/index.ts",
        "tsConfig": "libs/attendance/domain/tsconfig.lib.json",
        "assets": ["libs/attendance/domain/*.md"]
      }
    }
  }
}
