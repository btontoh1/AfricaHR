# Server-only (no NEXT_PUBLIC_ prefix) — the browser never talks to the
# NestJS API directly. Every request goes through this app's own Route
# Handlers (the BFF layer), which attach the httpOnly-cookie-stored auth
# token server-side before forwarding to this URL.
API_BASE_URL=http://localhost:3000/api
