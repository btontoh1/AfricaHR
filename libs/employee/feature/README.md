# employee-feature

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build employee-feature` to build the library.

## Running unit tests

Run `nx test employee-feature` to execute the unit tests via [Jest](https://jestjs.io).
