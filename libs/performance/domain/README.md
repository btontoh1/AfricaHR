# performance-domain

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build performance-domain` to build the library.

## Running unit tests

Run `nx test performance-domain` to execute the unit tests via [Jest](https://jestjs.io).
